# Architecture Overview

Medsure follows the MVVM (Model–View–ViewModel) architecture.

## Components

- Model: Handles medicine data and storage logic
- View: XML-based UI screens (conceptual)
- ViewModel: Handles business logic and data flow

This architecture ensures clean code structure and scalability.
